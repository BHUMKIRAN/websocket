/**
 * useSocket HOOK
 * Path: hooks/useSocket.ts
 *
 * TODO: Implement the following features:
 * - [ ] Initialize Socket.IO connection on mount
 * - [ ] Pass JWT token to socket authentication
 * - [ ] Connect to ws://localhost:8080
 * - [ ] Handle connection success
 * - [ ] Handle connection error
 * - [ ] Handle disconnect
 * - [ ] Auto-reconnect on disconnect
 * - [ ] Emit "chat:join" when joining a chat
 * - [ ] Emit "message:send" for sending messages
 * - [ ] Emit "message:edit" for editing messages
 * - [ ] Emit "message:delete" for deleting messages
 * - [ ] Emit "typing:start" when typing
 * - [ ] Emit "typing:stop" when stop typing
 * - [ ] Listen to "message:new" event
 * - [ ] Listen to "message:edited" event
 * - [ ] Listen to "message:deleted" event
 * - [ ] Listen to "typing:started" event
 * - [ ] Listen to "typing:stopped" event
 * - [ ] Listen to "user:online" event
 * - [ ] Listen to "user:offline" event
 * - [ ] Get socket connection status
 * - [ ] Return hook object with emit, on, off, status
 */

export default function useSocket() {
  return {};
}
